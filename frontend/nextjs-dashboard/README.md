# EMQX Interview Dashboard

Simple Next.js + TypeScript + Tailwind dashboard for interview presentation.

## Run

```bash
npm install
npm run dev
```

Open `http://localhost:3000`.

This demo currently uses mock API data from `app/api/cluster/route.ts`.
